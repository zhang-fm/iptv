name: IPTV 筛选与格式转换

on:
  schedule:
    - cron: '25,55 * * * *' 
  workflow_dispatch:

jobs:
  filter-and-convert:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    concurrency:
      group: iptv-sync
      cancel-in-progress: true

    steps:
      - name: 📥 Checkout 仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 设置 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 安装依赖
        run: pip install requests

      - name: 1. 运行速度筛选 (实时日志模式)
        env:
          PYTHONUNBUFFERED: "1"  # 强制实时输出日志
        run: python py/speed_filter.py

      - name: 2.1 转换为标准 M3U
        run: python py/convert_to_m3u.py
        continue-on-error: true  # 相当于之前的 || echo "跳过"

      - name: 2.2 生成自定义组播列表
        run: python py/zubo.pgen_custom_list.py
        continue-on-error: true

      - name: 2.3 生成全量 M3U 文件
        run: python py/convert_full_m3u.py
        continue-on-error: true

      - name: 📤 提交并推送更新
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"

          # 强制添加所有可能被脚本修改的文件，不使用模糊匹配以防报错
          # 确保包含 py/ 目录下的 txt 和 test/ 目录下的 m3u
          git add py/*.txt || true
          git add test/*.m3u || true
          git add *.m3u || true

          # 关键修改：检查所有变动（包括新文件）
          if git diff --cached --quiet; then
            echo "✅ 经对比，文件内容无任何实际变动，跳过推送。"
          else
            echo "🚀 检测到本地文件已更新，准备推送..."
            git commit -m "自动更新：线路维护与黑名单 ($(date +'%Y-%m-%d %H:%M'))"
            
            # 使用更安全的合并策略
            git pull --rebase -X ours origin main
            git push origin main
          fi
